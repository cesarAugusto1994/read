{% extends "::base.html.twig" %}

{% block title %}Secao{% endblock %}

{% block body %}

    <section id="page-breadcrumb">
        <div class="vertical-center sun">
            <div class="container">
                <div class="row">
                    <div class="action">
                        <div class="col-sm-12">
                            <h1 class="title">Menu</h1>
                            <a href="{{ path('read_secao_new') }}"><button class="btn btn-link">Cadastrar Novo</button></a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <table class="table table-hover table-condensed">
        <caption>Se&ccedil;&otilde;es</caption>
        <thead>
        <tr>
            <th>ID</th>
            <th>Nome</th>
        </tr>
        </thead>
        <tbody>
        {% for item in itens %}
            <tr>
                <td>{{ item.id }}</td>
                <td>{{ item.nome }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
