{% extends '::base.html.twig' %}

{% block body %}

    <!--
    <div class="well well-sm">
    <button class="btn btn-link">Cadastrar Novo</button>
    </div>
    -->

    <section id="page-breadcrumb">
        <div class="vertical-center sun">
            <div class="container">
                <div class="row">
                    <div class="action">
                        <div class="col-sm-12">
                            <h1 class="title">Menu</h1>
                            <a href="{{ path('read_acervo_new') }}"><button class="btn btn-link">Cadastrar Novo</button></a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <table class="table table-hover table-condensed">
        <caption>Meus Livros</caption>
        <thead>
        <tr>
            <th>ID</th>
            <th>Titulo</th>
            <th>Autor</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        {% for acervo in acervos %}
        <tr>
            <td>{{ acervo.id }}</td>
            <td>{{ acervo.titulo }}</td>
            <td>{{ acervo.autor.nome }}</td>
            <td><a href="{{ path('read_acervo_findone', {'id': acervo.id}) }}">Detalhes</a></td>
        </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}